(function () {
    var toPage = "<%= @mobile_redirect_path %>";
    if (toPage == "<%= root_path %>") {
       window.location.href = "#";
    } else {
       $.mobile.changePage(toPage, "slide", false, true);
    }
}());